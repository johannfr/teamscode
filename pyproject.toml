[project]
name = "teamscode"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "notify2>=0.3.1",
    "pyclip>=0.7.0",
    "python-dotenv>=1.1.1",
]
